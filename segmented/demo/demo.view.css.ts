namespace $.$$ {
	const { rem } = $mol_style_unit

	$mol_style_define( $eve_segmented_demo, {
		View_mode: {
			marginBottom: rem( 1 ),
		},

		Size_selector: {
			marginBottom: rem( 1 ),
		},

		Theme_selector: {
			marginBottom: rem( 1 ),
		},
	} )
}

