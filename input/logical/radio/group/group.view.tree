$eve_input_logical_radio_group $eve_flex
	direction? \column
	value? \
	name <= group_name \
	attr *
		role \radiogroup
	options *
	Option_ids /
	sub <= Options /
		<= Option* $eve_input_logical_radio_labelled
			name <= group_name
			value <= option_value* \
			checked? <= option_checked* false
			label <= option_title* \
			event_click?event <=> option_click*?event null
