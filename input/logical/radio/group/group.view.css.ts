namespace $.$$ {

	const { rem } = $mol_style_unit

	$mol_style_define( $eve_input_logical_radio_group, {
		
		flex: {
			direction: 'column',
			wrap: 'nowrap',
		},
		gap: rem( 0.5 ),
		align: {
			items: 'flex-start',
		},
		
	} )
	
}

